# Chapter 4: Passive Reconnaissance Agents
# Environment configuration

# Output path for reconnaissance artifacts (default: runs/recon.jsonl)
RECON_OUT=runs/recon.jsonl

# Default timeout for network operations in seconds
RECON_TIMEOUT=4

# Default User-Agent string
RECON_USER_AGENT=ReconSnap/1.0

# Scope configuration file path
SCOPE_CONFIG=data/scope.json

# Enable/disable safety gates (set to "false" to bypass in controlled environments)
ENABLE_SCOPE_CHECK=true
ENABLE_TIME_GATE=false

# Time window for operations (24-hour format, UTC)
# Only used if ENABLE_TIME_GATE=true
TIME_WINDOW_START=9
TIME_WINDOW_END=17

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
